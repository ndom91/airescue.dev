<div class="container">
  <section class="hero">
    <div class="hero-grid">
      <div class="hero-copy">
        <div class="hero-eyebrow">Production-grade engineering for AI-built products</div>
        <h1>You shipped fast.<br /><span class="dim">We'll make it last.</span></h1>
        <p class="hero-desc">AIrescue turns rapid prototypes and AI-built codebases into production-grade systems your team can run, scale, and ship from confidently.</p>
        <div class="hero-actions">
          <a href="#" class="btn btn-accent">Claim free assessment slot</a>
          <a href="#services" class="btn btn-ghost">See what we cover</a>
        </div>
        <p class="hero-meta mono">Avg first response: 2 hours • Typical project: 5-10 days</p>
      </div>
      <aside class="hero-console" aria-label="Sample rescue diagnostics">
        <div class="console-head">
          <span class="mono">triage.log</span>
          <span class="status">live</span>
        </div>
        <ul>
          <li><span class="mono">[found]</span> auth tokens exposed — rotating now</li>
          <li><span class="mono">[found]</span> 12 circular imports — untangling</li>
          <li><span class="mono">[found]</span> payment flow needs rollback path</li>
          <li><span class="mono">[found]</span> 18 unused deps — trimming bundle</li>
          <li class="good"><span class="mono">[done]</span> hardening patch merged + tests green</li>
        </ul>
      </aside>
    </div>
  </section>
</div>

<style>
  @keyframes heroFadeUp {
    from {
      opacity: 0;
      transform: translateY(22px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes scan {
    from {
      transform: translateY(-100%);
    }
    to {
      transform: translateY(430%);
    }
  }

  .hero {
    padding: 9rem 0 7rem;
  }

  .hero-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.25fr) minmax(0, 0.85fr);
    gap: 1.75rem;
    align-items: start;
  }

  .hero-copy {
    max-width: 760px;
    position: relative;
    z-index: 2;
  }

  .hero-eyebrow {
    font-family: var(--font-mono);
    display: inline-block;
    font-size: 0.72rem;
    color: var(--text);
    margin-bottom: 2rem;
    letter-spacing: 0.09em;
    text-transform: uppercase;
    padding: 0.24rem 0.62rem;
    border: 1px solid var(--ink);
    box-shadow: 3px 3px 0 var(--border);
    background: var(--surface);
    animation: heroFadeUp 0.6s ease 0.05s both;
  }

  .hero h1 {
    font-size: clamp(2.8rem, 6.5vw, 5.8rem);
    font-weight: 800;
    line-height: 0.84;
    letter-spacing: -0.065em;
    color: var(--text);
    margin-bottom: 2.5rem;
    animation: heroFadeUp 0.62s ease 0.12s both;
    white-space: nowrap;
  }

  .hero h1 .dim {
    background: linear-gradient(120deg, var(--text-dim), var(--hero-dim-end));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-desc {
    font-size: 1.03rem;
    color: var(--text-dim);
    max-width: 560px;
    margin-bottom: 2.5rem;
    line-height: 1.55;
    animation: heroFadeUp 0.62s ease 0.24s both;
  }

  .hero-actions {
    display: flex;
    gap: 0.85rem;
    margin-bottom: 1.2rem;
    animation: heroFadeUp 0.62s ease 0.34s both;
  }

  .hero-meta {
    font-size: 0.75rem;
    color: var(--text-dim);
    letter-spacing: 0.04em;
    animation: heroFadeUp 0.62s ease 0.45s both;
  }

  .hero-console {
    background: var(--surface);
    border: 2px solid var(--ink);
    box-shadow: 9px 9px 0 var(--border-strong);
    position: relative;
    overflow: hidden;
    animation: heroFadeUp 0.7s ease 0.32s both;
    margin-top: 3.2rem;
    transform: translateX(1rem);
  }


  .hero-console::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(to bottom, transparent, var(--scan-line), transparent);
    animation: scan 7s linear infinite;
    pointer-events: none;
  }

  .console-head {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.55rem 0.85rem;
    border-bottom: 2px solid var(--ink);
    background: var(--surface-2);
    position: relative;
    z-index: 1;
  }

  .console-head span {
    font-size: 0.72rem;
    color: var(--text-dim);
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }

  .status {
    color: var(--danger);
    font-family: var(--font-mono);
  }

  .hero-console ul {
    list-style: none;
    padding: 0.95rem 1rem;
    position: relative;
    z-index: 1;
  }

  .hero-console li {
    font-size: 0.8rem;
    color: var(--text);
    line-height: 1.5;
    padding: 0.48rem 0;
    border-bottom: 1px dashed var(--border);
  }

  .hero-console li:last-child {
    border-bottom: 0;
  }

  .hero-console .mono {
    color: var(--text);
    font-size: 0.72rem;
    margin-right: 0.35rem;
  }

  .hero-console .good {
    color: var(--accent-ink-strong);
  }

  @media (max-width: 960px) {
    .hero {
      padding-top: 3rem;
    }

    .hero-grid {
      grid-template-columns: 1fr;
    }

    .hero-console {
      max-width: 580px;
      transform: none;
      margin-top: 1.2rem;
    }
  }

  @media (max-width: 720px) {
    .hero {
      padding-top: 2.5rem;
    }

    .hero h1 {
      line-height: 0.9;
      white-space: normal;
    }

    .hero-actions {
      flex-direction: column;
    }

    .hero-actions a {
      width: 100%;
    }
  }
</style>
