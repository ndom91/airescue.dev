<div class="container">
  <section class="hero">
    <div class="hero-eyebrow">// for codebases that outgrew the prompts that built them</div>
    <h1>You shipped fast.<br><span class="dim">We'll make it last.</span></h1>
    <p class="hero-desc">You moved fast and got something real out the door. Now it needs a proper foundation. We're the engineers who turn AI-generated code into production-grade software.</p>
    <div class="hero-actions">
      <a href="#" class="btn btn-accent">Get a free assessment &rarr;</a>
      <a href="#services" class="btn btn-ghost">What we fix</a>
    </div>
  </section>
</div>

<style>
  @keyframes heroFadeUp {
    from { opacity: 0; transform: translateY(32px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }

  .container {
    position: relative;
  }

  .container::after {
    content: '';
    position: absolute;
    inset: 0;
    background:
      linear-gradient(rgba(212, 255, 43, 0.02) 1px, transparent 1px),
      linear-gradient(90deg, rgba(212, 255, 43, 0.02) 1px, transparent 1px);
    background-size: 60px 60px;
    mask-image: linear-gradient(to bottom, black 40%, transparent 90%);
    -webkit-mask-image: linear-gradient(to bottom, black 40%, transparent 90%);
    pointer-events: none;
    z-index: 0;
  }

  .hero {
    padding: 11rem 0 6rem;
    position: relative;
    z-index: 1;
  }

  .hero::before {
    content: '';
    display: block;
    width: 60px;
    height: 3px;
    background: var(--accent);
    margin-bottom: 1.5rem;
    border-radius: 1px;
  }

  .hero-eyebrow {
    font-family: 'DM Mono', monospace;
    font-size: 0.8rem;
    color: var(--accent);
    margin-bottom: 1.5rem;
    letter-spacing: 0.02em;
    animation: heroFadeUp 0.7s cubic-bezier(0.16, 1, 0.3, 1) 0.1s both;
  }

  .hero-eyebrow::after {
    content: '|';
    display: inline-block;
    margin-left: 2px;
    color: var(--accent);
    animation: blink 1s step-end infinite;
  }

  .hero h1 {
    font-size: clamp(3.5rem, 8vw, 6.5rem);
    font-weight: 800;
    line-height: 0.95;
    letter-spacing: -0.045em;
    color: var(--white);
    margin-bottom: 2rem;
    text-wrap: balance;
    animation: heroFadeUp 0.7s cubic-bezier(0.16, 1, 0.3, 1) 0.2s both;
  }

  .hero h1 .dim {
    background: linear-gradient(180deg, var(--text-dim) 0%, rgba(113, 113, 122, 0.35) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-desc {
    font-size: 1.1rem;
    color: var(--text-dim);
    max-width: 520px;
    margin-bottom: 2.5rem;
    line-height: 1.7;
    animation: heroFadeUp 0.7s cubic-bezier(0.16, 1, 0.3, 1) 0.35s both;
  }

  .hero-actions {
    display: flex;
    gap: 0.75rem;
    animation: heroFadeUp 0.7s cubic-bezier(0.16, 1, 0.3, 1) 0.5s both;
  }

  @media (max-width: 600px) {
    .hero {
      padding: 8rem 0 4rem;
    }

    .hero h1 {
      line-height: 1;
    }

    .hero-actions {
      flex-direction: column;
    }
  }
</style>
