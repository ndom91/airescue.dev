<div class="container">
  <section class="proof">
    <div class="section-header">
      <div class="eyebrow">// field reports</div>
      <h2>They were stuck too</h2>
    </div>
    <div class="proof-grid">
      <div class="proof-card">
        <blockquote>"I spent 3 weeks in a prompt loop. Every fix created new bugs. AIrescue <strong>fixed everything in 4 days</strong> and I finally understand my own codebase."</blockquote>
        <div class="proof-author">
          <strong>Alex T.</strong> &middot; Solo Founder
        </div>
      </div>
      <div class="proof-card">
        <blockquote>"Our contractor vibe-coded the entire backend. When they left, we were stuck. AIrescue <strong>made the code ours again</strong>&mdash;with tests, types, and documentation."</blockquote>
        <div class="proof-author">
          <strong>Priya K.</strong> &middot; Startup CTO
        </div>
      </div>
      <div class="proof-card">
        <blockquote>"They found <strong>SQL injection in production</strong>. The AI had generated raw query strings everywhere. That alone was worth 10x the price."</blockquote>
        <div class="proof-author">
          <strong>Marcus J.</strong> &middot; Engineering Lead
        </div>
      </div>
    </div>
  </section>
</div>

<style>
  .proof {
    padding: 5.5rem 0;
    animation: fadeSlideIn ease both;
    animation-timeline: view();
    animation-range: entry 0% entry 20%;
  }

  .proof-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
    align-items: start;
  }

  .proof-card {
    background: var(--surface);
    border: 2px solid var(--ink);
    padding: 2.2rem 1.8rem 1.6rem;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    position: relative;
    overflow: hidden;
    transition: border-color 0.3s, transform 0.3s, box-shadow 0.3s;
    box-shadow: 6px 6px 0 var(--border-strong);
  }

  .proof-card::before {
    content: '\201C';
    position: absolute;
    top: -10px;
    right: 12px;
    font-size: 8rem;
    font-family: var(--font-sans);
    font-weight: 700;
    color: var(--text-faint);
    line-height: 1;
    pointer-events: none;
    transition: color 0.3s;
  }

  .proof-card:hover {
    border-color: var(--ink);
    transform: translate(-3px, -3px);
    box-shadow: 9px 9px 0 var(--ink);
  }

  .proof-card:hover::before {
    color: var(--accent-ghost);
  }

  .proof-card:nth-child(2) {
    transform: translateY(-8px) rotate(-0.8deg);
    background: color-mix(in srgb, var(--surface) 82%, var(--surface-2) 18%);
  }

  .proof-card:nth-child(2):hover {
    transform: translateY(-12px) rotate(-0.8deg);
  }

  .proof-card blockquote {
    font-size: 0.92rem;
    color: var(--text);
    line-height: 1.7;
    margin-bottom: 1.5rem;
    position: relative;
    z-index: 1;
  }

  .proof-card blockquote strong {
    color: var(--accent-ink);
    font-weight: 600;
  }

  .proof-author {
    font-size: 0.8rem;
    color: var(--text-dim);
    padding-top: 1rem;
    border-top: 1px dashed var(--border);
  }

  .proof-author strong {
    color: var(--text);
    font-weight: 600;
  }

  @media (max-width: 900px) {
    .proof-grid {
      grid-template-columns: 1fr;
    }

    .proof-card:nth-child(2) {
      transform: none;
    }

    .proof-card:nth-child(2):hover {
      transform: translateY(-4px);
    }
  }
</style>
