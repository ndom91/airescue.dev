<section class="cta-section">
  <div class="container">
    <div class="cta">
      <div class="cta-alert mono" aria-hidden="true">Response window closes today</div>
      <div class="cta-eyebrow">// dispatch a rescue engineer</div>
      <h2>Ready for code you can trust?<br />Start <span class="accent">shipping.</span></h2>
      <p>You already proved demand. Now lock in the engineering quality to scale without fear.</p>
      <div class="cta-actions">
        <a href="#" class="btn btn-accent btn-lg">Claim your assessment slot</a>
        <a href="#" class="btn btn-ghost">View case studies</a>
      </div>
      <div class="cta-deco" aria-hidden="true">SOS</div>
    </div>
  </div>
</section>

<style>
  .cta-section {
    position: relative;
    animation: fadeSlideIn ease both;
    animation-timeline: view();
    animation-range: entry 0% entry 20%;
  }

  .cta-section .container {
    padding-top: 4rem;
    padding-bottom: 4rem;
  }

  .cta {
    padding: 3.5rem 0 2.8rem;
    text-align: left;
    border: 2px solid var(--ink);
    background: var(--surface);
    box-shadow: 10px 10px 0 var(--ink);
    position: relative;
    overflow: hidden;
    padding-left: clamp(1.5rem, 5vw, 4rem);
    padding-right: clamp(1.5rem, 5vw, 4rem);
  }

  .cta-alert {
    display: inline-block;
    margin-bottom: 1rem;
    font-size: 0.64rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--warn-ink);
    background: var(--warn-bg);
    border: 1px solid var(--ink);
    box-shadow: 3px 3px 0 var(--ink);
    padding: 0.22rem 0.55rem;
  }

  .cta::after {
    content: '';
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0;
    height: 8px;
    background-image: repeating-linear-gradient(-45deg, var(--stripe-dark), var(--stripe-dark) 8px, var(--warn-bg) 8px, var(--warn-bg) 16px);
    pointer-events: none;
  }

  .cta-eyebrow {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text-dim);
    margin-bottom: 1.3rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .cta h2 {
    font-size: clamp(2.2rem, 5vw, 3.8rem);
    font-weight: 800;
    color: var(--text);
    letter-spacing: -0.05em;
    line-height: 0.93;
    margin-bottom: 1.1rem;
    max-width: 18ch;
  }

  .cta h2 .accent {
    background: linear-gradient(135deg, var(--accent-grad-start), var(--accent-grad-end));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .cta p {
    font-size: 0.99rem;
    color: var(--text-dim);
    max-width: 46ch;
    margin: 0 0 2rem;
    line-height: 1.6;
  }

  .cta-actions {
    display: flex;
    gap: 0.85rem;
    justify-content: flex-start;
  }

  .btn-lg {
    padding: 0.88rem 1.95rem;
  }


  .cta-deco {
    position: absolute;
    right: clamp(1rem, 4vw, 4rem);
    top: 50%;
    transform: translateY(-50%);
    font-family: var(--font-sans);
    font-size: clamp(6rem, 14vw, 12rem);
    font-weight: 800;
    letter-spacing: -0.06em;
    line-height: 0.85;
    color: transparent;
    -webkit-text-stroke: 2px var(--text-faint-strong);
    pointer-events: none;
    user-select: none;
  }

  @media (max-width: 720px) {
    .cta {
      padding: 3.5rem 1.2rem 3.2rem;
    }

    .cta-deco {
      display: none;
    }

    .cta-actions {
      flex-direction: column;
      align-items: stretch;
    }

    .cta-actions .btn {
      width: 100%;
    }
  }
</style>
