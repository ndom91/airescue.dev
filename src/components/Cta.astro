<div class="container">
  <section class="cta">
    <h2>Stop prompting in circles.<br>Start <span class="accent">shipping.</span></h2>
    <p>Your codebase won't fix itself. And the AI that broke it can't save it. Talk to an engineer.</p>
    <div class="cta-actions">
      <a href="#" class="btn btn-accent">Get a free assessment &rarr;</a>
      <a href="#" class="btn btn-ghost">View case studies</a>
    </div>
  </section>
</div>

<style>
  .cta {
    padding: 8rem 0;
    text-align: center;
    border-top: 1px solid var(--border);
  }

  .cta h2 {
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 800;
    color: var(--white);
    letter-spacing: -0.035em;
    line-height: 1.1;
    margin-bottom: 1.25rem;
  }

  .cta h2 .accent {
    color: var(--accent);
  }

  .cta p {
    font-size: 1.05rem;
    color: var(--text-dim);
    max-width: 460px;
    margin: 0 auto 2.5rem;
    line-height: 1.7;
  }

  .cta-actions {
    display: flex;
    gap: 0.75rem;
    justify-content: center;
  }

  @media (max-width: 600px) {
    .cta-actions {
      flex-direction: column;
      align-items: center;
    }
  }
</style>
