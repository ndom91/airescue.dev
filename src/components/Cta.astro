<div class="container">
  <section class="cta">
    <h2>Ready for code you can trust?<br>Start <span class="accent">shipping.</span></h2>
    <p>You built something worth investing in. Let's give it the engineering foundation it deserves.</p>
    <div class="cta-actions">
      <a href="#" class="btn btn-accent">Get a free assessment &rarr;</a>
      <a href="#" class="btn btn-ghost">View case studies</a>
    </div>
  </section>
</div>

<style>
  .cta {
    padding: 10rem 0 8rem;
    text-align: center;
    position: relative;
    animation: fadeSlideIn ease both;
    animation-timeline: view();
    animation-range: entry 0% entry 20%;
  }

  .cta::before {
    content: '';
    position: absolute;
    top: 0;
    left: 10%;
    right: 10%;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--accent-mid), transparent);
  }

  .cta h2 {
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 800;
    color: var(--white);
    letter-spacing: -0.035em;
    line-height: 1.1;
    margin-bottom: 1.25rem;
  }

  .cta h2 .accent {
    background: linear-gradient(135deg, var(--accent), #a8ff00);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .cta p {
    font-size: 1.05rem;
    color: var(--text-dim);
    max-width: 460px;
    margin: 0 auto 2.5rem;
    line-height: 1.7;
  }

  .cta-actions {
    display: flex;
    gap: 0.75rem;
    justify-content: center;
  }

  @media (max-width: 600px) {
    .cta-actions {
      flex-direction: column;
      align-items: center;
    }
  }
</style>
