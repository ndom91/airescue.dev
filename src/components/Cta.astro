<section class="cta-section">
  <div class="container">
    <div class="cta">
      <div class="cta-eyebrow">// dispatch a rescue engineer</div>
      <h2>Ready for code you can trust?<br />Start <span class="accent">shipping.</span></h2>
      <p>You already proved demand. Now lock in the engineering quality to scale without fear.</p>
      <div class="cta-actions">
        <a href="#" class="btn btn-accent btn-lg">Get a free assessment</a>
        <a href="#" class="btn btn-ghost">View case studies</a>
      </div>
    </div>
  </div>
</section>

<style>
  .cta-section {
    position: relative;
    animation: fadeSlideIn ease both;
    animation-timeline: view();
    animation-range: entry 0% entry 20%;
  }

  .cta-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: var(--border);
  }

  .cta {
    padding: 8.2rem 0 7.2rem;
    text-align: center;
    border: 1px solid var(--border-strong);
    background: linear-gradient(145deg, var(--surface), var(--surface-2));
    box-shadow: 9px 9px 0 var(--border);
    position: relative;
    overflow: hidden;
  }

  .cta::after {
    content: '';
    position: absolute;
    width: 220px;
    height: 220px;
    right: -70px;
    top: -70px;
    border: 1px dashed var(--border-strong);
    border-radius: 50%;
    pointer-events: none;
  }

  .cta-eyebrow {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text-dim);
    margin-bottom: 1.3rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .cta h2 {
    font-size: clamp(2.25rem, 5.5vw, 4rem);
    font-weight: 700;
    color: var(--text);
    letter-spacing: -0.045em;
    line-height: 1.05;
    margin-bottom: 1.5rem;
  }

  .cta h2 .accent {
    background: linear-gradient(135deg, var(--accent-grad-start), var(--accent-grad-end));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .cta p {
    font-size: 1.03rem;
    color: var(--text-dim);
    max-width: 480px;
    margin: 0 auto 3rem;
    line-height: 1.7;
  }

  .cta-actions {
    display: flex;
    gap: 0.85rem;
    justify-content: center;
  }

  .btn-lg {
    padding: 0.8rem 1.85rem;
  }

  @media (max-width: 720px) {
    .cta {
      padding: 6.2rem 1rem 5.6rem;
    }

    .cta-actions {
      flex-direction: column;
      align-items: stretch;
    }

    .cta-actions .btn {
      width: 100%;
    }
  }
</style>
