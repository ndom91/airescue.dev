<div class="container" id="process">
  <section class="process">
    <div class="section-header">
      <div class="eyebrow">// operating sequence</div>
      <h2>Four steps to a codebase you actually own</h2>
    </div>
    <div class="process-track">
      <div class="process-step">
        <div class="step-num">01</div>
        <div class="step-dot"></div>
        <div class="step-label">Intake</div>
        <h3>Share your repo</h3>
        <p>Send us a link and tell us where you want to go. We respond within 2 hours with an honest assessment.</p>
        <span class="time">~2 hours</span>
      </div>
      <div class="process-step">
        <div class="step-num">02</div>
        <div class="step-dot"></div>
        <div class="step-label">Diagnose</div>
        <h3>Deep code audit</h3>
        <p>A senior engineer reads every file, maps the architecture, and builds a prioritized plan for what to strengthen first.</p>
        <span class="time">~24 hours</span>
      </div>
      <div class="process-step">
        <div class="step-num">03</div>
        <div class="step-dot"></div>
        <div class="step-label">Operate</div>
        <h3>Fix, refactor, test</h3>
        <p>We tackle the highest-impact items first, then restructure and add tests. You get daily progress updates.</p>
        <span class="time">~5–8 days</span>
      </div>
      <div class="process-step">
        <div class="step-num">04</div>
        <div class="step-dot"></div>
        <div class="step-label">Handoff</div>
        <h3>You own it</h3>
        <p>Walkthrough call, full documentation, and 2 weeks of support. Clean, tested code that your team can maintain.</p>
        <span class="time">~1hr + 2 weeks</span>
      </div>
    </div>
  </section>
</div>

<style>
  @keyframes pulse-ring {
    0%, 100% {
      transform: scale(1);
      opacity: 0.5;
    }
    50% {
      transform: scale(1.8);
      opacity: 0;
    }
  }

  .process {
    padding: 5.5rem 0 5.5rem;
    animation: fadeSlideIn ease both;
    animation-timeline: view();
    animation-range: entry 0% entry 20%;
  }

  .process-track {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2px;
    background: var(--ink);
    border: 2px solid var(--ink);
    box-shadow: 7px 7px 0 var(--border-strong);
  }

  .process-step {
    padding: 1.6rem;
    background: var(--surface);
    position: relative;
  }

  .process-step::before {
    content: '';
    position: absolute;
    top: 3.4rem;
    left: 0.25rem;
    right: 0;
    height: 2px;
    background: repeating-linear-gradient(90deg, var(--border-strong) 0, var(--border-strong) 6px, transparent 6px, transparent 12px);
  }

  .process-step:last-child::before {
    right: unset;
    width: 3rem;
  }

  .step-num {
    font-family: var(--font-mono);
    font-size: 2.3rem;
    font-weight: 500;
    color: var(--text-faint-strong);
    line-height: 1;
    letter-spacing: -0.05em;
    margin-bottom: 0.25rem;
  }

  .step-dot {
    width: 10px;
    height: 10px;
    background: var(--text);
    margin-bottom: 1.2rem;
    position: relative;
    z-index: 1;
  }

  .step-dot::after {
    content: '';
    position: absolute;
    inset: -5px;
    border: 1px solid var(--accent-mid);
  }

  .process-step:nth-child(2) .step-dot::after { animation-delay: 0.5s; }
  .process-step:nth-child(3) .step-dot::after { animation-delay: 1s; }
  .process-step:nth-child(4) .step-dot::after { animation-delay: 1.5s; }

  .process-step .step-label {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--text-dim);
    margin-bottom: 0.5rem;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .process-step h3 {
    font-size: 1.1rem;
    font-weight: 600;
    color: var(--text);
    margin-bottom: 0.5rem;
    letter-spacing: -0.02em;
  }

  .process-step p {
    font-size: 0.85rem;
    color: var(--text-dim);
    line-height: 1.65;
  }

  .process-step .time {
    display: inline-block;
    margin-top: 1rem;
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--accent-ink);
    padding: 0.25rem 0.6rem;
    border: 1px solid var(--accent-mid);
    background: color-mix(in srgb, var(--accent) 16%, var(--white) 84%);
  }

  @media (max-width: 720px) {
    .process {
      padding: 3rem 0;
    }
  }

  @media (max-width: 900px) {
    .process-track {
      grid-template-columns: 1fr 1fr;
    }

    .process-step::before {
      display: none;
    }
  }

  @media (max-width: 600px) {
    .process-track {
      grid-template-columns: 1fr;
    }
  }
</style>
