<section class="trust">
  <div class="container">
    <div class="trust-inner">
      <div class="trust-label mono">
        <strong>2024</strong>
        Field Notes Since
      </div>
      <div class="trust-stats">
        <div class="trust-stat">
          <div class="num">340<span>+</span></div>
          <div class="lbl">Codebases rescued</div>
        </div>
        <div class="trust-stat">
          <div class="num">48<span>h</span></div>
          <div class="lbl">Avg. turnaround</div>
        </div>
        <div class="trust-stat">
          <div class="num">12<span>k</span></div>
          <div class="lbl">Bugs squashed</div>
        </div>
        <div class="trust-stat">
          <div class="num">98<span>%</span></div>
          <div class="lbl">Satisfaction</div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .trust {
    padding: 4rem 0 3.6rem;
  }

  .trust-inner {
    display: flex;
    align-items: stretch;
    gap: 0;
    border: 2px solid var(--ink);
    background: var(--surface);
    box-shadow: 6px 6px 0 var(--border-strong);
  }

  .trust-label {
    font-size: 0.77rem;
    text-align: left;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--text-dim);
    min-width: 180px;
    display: grid;
    align-content: end;
    justify-items: start;
    border-right: 2px solid var(--ink);
    padding: 1rem 1.2rem;
    background: var(--accent);
  }

  .trust-label strong {
    display: block;
    font-family: inherit;
    font-size: 2rem;
    font-weight: 700;
    color: var(--text);
    letter-spacing: -0.04em;
    font-variant-numeric: tabular-nums;
    line-height: 1;
    margin-bottom: 0.35rem;
  }

  .trust-stats {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    width: 100%;
  }

  .trust-stat {
    white-space: nowrap;
    padding: 1rem 1.2rem;
    border-right: 1px solid var(--border-strong);
  }

  .trust-stat:last-child {
    border-right: 0;
  }

  .trust-stat .num {
    font-size: 2rem;
    font-weight: 700;
    color: var(--text);
    letter-spacing: -0.04em;
    font-variant-numeric: tabular-nums;
    line-height: 1;
    margin-bottom: 0.35rem;
  }

  .trust-stat .num span {
    color: var(--accent-ink);
  }

  .trust-stat .lbl {
    font-size: 0.77rem;
    color: var(--text-dim);
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }

  @media (max-width: 960px) {
    .trust-inner {
      flex-direction: column;
    }

    .trust-label {
      border-right: 0;
      border-bottom: 1px solid var(--border);
      width: 100%;
      place-items: start;
    }

    .trust-stats {
      grid-template-columns: 1fr 1fr;
    }

    .trust-stat:nth-child(2) {
      border-right: 0;
    }

    .trust-stat:nth-child(-n + 2) {
      border-bottom: 1px solid var(--border);
    }
  }

  @media (max-width: 520px) {
    .trust-stats {
      grid-template-columns: 1fr;
    }

    .trust-stat {
      border-right: 0;
      border-bottom: 1px solid var(--border);
    }

    .trust-stat:last-child {
      border-bottom: 0;
    }
  }
</style>
