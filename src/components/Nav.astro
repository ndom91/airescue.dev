<nav aria-label="Main navigation">
  <div class="nav-inner">
    <a href="/" class="logo"><span class="logo-ai">AI</span>rescue<span class="logo-dot">.</span><span class="logo-tld">dev</span></a>
    <div class="nav-right">
      <a href="#services">Services</a>
      <a href="#process">Process</a>
      <a href="#pricing">Pricing</a>
      <a href="#" class="btn btn-accent">Fix my code</a>
    </div>
  </div>
</nav>

<style>
  nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    padding: 0 2rem;
    background: rgba(9, 9, 11, 0.8);
    backdrop-filter: blur(16px);
    border-bottom: 1px solid var(--border);
  }

  .nav-inner {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    height: 56px;
  }

  .logo {
    font-size: 1.05rem;
    font-weight: 800;
    color: var(--white);
    text-decoration: none;
    letter-spacing: -0.03em;
    display: flex;
    align-items: center;
    gap: 0.1rem;
  }

  .logo-ai {
    font-family: 'DM Mono', monospace;
    font-size: 0.65rem;
    font-weight: 500;
    letter-spacing: 0.05em;
    color: var(--bg);
    background: var(--accent);
    padding: 0.2rem 0.3rem;
    border-radius: 3px;
    margin-right: 0.15rem;
    line-height: 1.25;
    position: relative;
    top: -0.5px;
  }

  .logo-dot {
    color: var(--accent);
  }

  .logo-tld {
    color: var(--text-dim);
    font-weight: 600;
  }

  .nav-right {
    display: flex;
    align-items: center;
    gap: 2rem;
  }

  .nav-right a:not(.btn) {
    color: var(--text-dim);
    text-decoration: none;
    font-size: 0.85rem;
    font-weight: 500;
    transition: color 0.15s;
    position: relative;
  }

  .nav-right a:not(.btn)::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 0;
    height: 1.5px;
    background: var(--accent);
    transition: width 0.2s ease;
  }

  .nav-right a:not(.btn):hover::after {
    width: 100%;
  }

  .nav-right a:not(.btn):hover {
    color: var(--white);
  }

  @media (max-width: 600px) {
    nav {
      padding: 0 1.25rem;
    }

    .nav-right a:not(.btn) {
      display: none;
    }
  }
</style>
