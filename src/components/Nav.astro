<nav aria-label="Main navigation">
  <div class="nav-inner">
    <a href="/" class="logo"><span class="logo-ai">AI</span>rescue<span class="logo-dot">.dev</span></a>
    <div class="nav-right">
      <a href="#services">Services</a>
      <a href="#process">Process</a>
      <a href="#pricing">Pricing</a>
      <a href="#" class="btn btn-accent nav-cta">Run triage</a>
    </div>
  </div>
</nav>

<style>
  nav {
    position: fixed;
    top: 0.75rem;
    left: 0;
    right: 0;
    z-index: 100;
    padding: 0 2rem;
  }

  .nav-inner {
    max-width: 1240px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    min-height: 3.5rem;
    padding: 0.35rem 0.45rem 0.35rem 0.8rem;
    border: 2px solid var(--ink);
    background: color-mix(in srgb, var(--surface) 92%, var(--white) 8%);
    box-shadow: 5px 5px 0 var(--border-strong);
  }

  .logo {
    font-size: 1.05rem;
    font-weight: 700;
    color: var(--text);
    text-decoration: none;
    letter-spacing: -0.035em;
    display: flex;
    align-items: center;
    gap: 0.2rem;
  }

  .logo-ai {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    color: var(--text);
    background: var(--accent);
    padding: 0.2rem 0.34rem;
    line-height: 1.2;
    border: 1px solid var(--text);
  }

  .logo-dot {
    color: var(--text-dim);
    font-size: 0.95rem;
  }

  .nav-right {
    display: flex;
    align-items: center;
    gap: 1.4rem;
  }

  .nav-right a:not(.btn) {
    color: var(--text-dim);
    text-decoration: none;
    font-family: var(--font-mono);
    font-size: 0.72rem;
    font-weight: 500;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    transition: color 0.2s ease;
    position: relative;
  }

  .nav-right a:not(.btn)::after {
    content: '';
    position: absolute;
    bottom: -3px;
    left: 0;
    width: 0;
    height: 1px;
    background: var(--text);
    transition: width 0.2s ease;
  }

  .nav-right a:not(.btn):hover::after {
    width: 100%;
  }

  .nav-right a:not(.btn):hover {
    color: var(--text);
  }

  .nav-cta {
    border-color: var(--accent);
    box-shadow: 4px 4px 0 var(--accent-ink);
  }

  .nav-cta:hover {
    border-color: var(--accent);
    box-shadow: 6px 6px 0 var(--accent-ink);
  }

  @media (max-width: 720px) {
    nav {
      padding: 0 1.1rem;
      top: 0.5rem;
    }

    .nav-right a:not(.btn) {
      display: none;
    }

    .nav-inner {
      padding-right: 0.3rem;
    }
  }
</style>
