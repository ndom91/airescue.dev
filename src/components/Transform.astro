<div class="container">
  <section class="transform">
    <div class="section-header">
      <div class="eyebrow">// before and after</div>
      <h2>What rescue actually looks like</h2>
    </div>
    <div class="transform-grid">
      <div class="transform-panel panel-before">
        <div class="transform-panel-head">
          <span>your-project/</span>
          <span class="tag tag-red">Before</span>
        </div>
        <div class="transform-panel-body">
          <div class="t-row">
            <div class="t-icon t-icon-bad">&#10005;</div>
            <div><span class="label">847 files</span> <span class="desc">&mdash; 40% unused or duplicated</span></div>
          </div>
          <div class="t-row">
            <div class="t-icon t-icon-bad">&#10005;</div>
            <div><span class="label">0 tests</span> <span class="desc">&mdash; no types, no linting</span></div>
          </div>
          <div class="t-row">
            <div class="t-icon t-icon-bad">&#10005;</div>
            <div><span class="label">12 circular deps</span> <span class="desc">&mdash; cryptic runtime errors</span></div>
          </div>
          <div class="t-row">
            <div class="t-icon t-icon-bad">&#10005;</div>
            <div><span class="label">SQL injection</span> <span class="desc">&mdash; in 3 endpoints</span></div>
          </div>
          <div class="t-row">
            <div class="t-icon t-icon-bad">&#10005;</div>
            <div><span class="label">No error handling</span> <span class="desc">&mdash; crashes silently</span></div>
          </div>
          <div class="t-row">
            <div class="t-icon t-icon-bad">&#10005;</div>
            <div><span class="label">8s page load</span> <span class="desc">&mdash; no code splitting</span></div>
          </div>
        </div>
      </div>
      <div class="transform-panel panel-after">
        <div class="transform-panel-head">
          <span>your-project/</span>
          <span class="tag tag-green">After</span>
        </div>
        <div class="transform-panel-body">
          <div class="t-row">
            <div class="t-icon t-icon-good">&#10003;</div>
            <div><span class="label">312 files</span> <span class="desc">&mdash; clean module boundaries</span></div>
          </div>
          <div class="t-row">
            <div class="t-icon t-icon-good">&#10003;</div>
            <div><span class="label">94% coverage</span> <span class="desc">&mdash; strict TS, ESLint, Prettier</span></div>
          </div>
          <div class="t-row">
            <div class="t-icon t-icon-good">&#10003;</div>
            <div><span class="label">0 circular deps</span> <span class="desc">&mdash; clear dependency graph</span></div>
          </div>
          <div class="t-row">
            <div class="t-icon t-icon-good">&#10003;</div>
            <div><span class="label">Parameterized queries</span> <span class="desc">&mdash; input validated</span></div>
          </div>
          <div class="t-row">
            <div class="t-icon t-icon-good">&#10003;</div>
            <div><span class="label">Structured errors</span> <span class="desc">&mdash; with monitoring</span></div>
          </div>
          <div class="t-row">
            <div class="t-icon t-icon-good">&#10003;</div>
            <div><span class="label">1.2s load</span> <span class="desc">&mdash; Lighthouse 97</span></div>
          </div>
        </div>
      </div>
    </div>
  </section>
</div>

<style>
  .transform {
    padding: 5.5rem 0 5.5rem;
    animation: fadeSlideIn ease both;
    animation-timeline: view();
    animation-range: entry 0% entry 20%;
  }

  .transform-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.25rem;
  }

  .transform-panel {
    border: 2px solid var(--ink);
    overflow: hidden;
    transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1), box-shadow 0.3s;
    box-shadow: 6px 6px 0 var(--border-strong);
    background: var(--surface);
  }

  .transform-panel:hover {
    transform: translate(-3px, -3px);
    box-shadow: 9px 9px 0 var(--ink);
  }

  .transform-panel-head {
    padding: 0.75rem 1.25rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 2px solid var(--ink);
    background: var(--surface-2);
  }

  .transform-panel-head::before {
    content: '';
    display: inline-block;
    width: 8px;
    height: 8px;
    background: var(--border-strong);
    box-shadow: 14px 0 0 var(--border-strong), 28px 0 0 var(--border-strong);
    opacity: 0.6;
    margin-right: 1rem;
    flex-shrink: 0;
  }

  .panel-before .transform-panel-head::before {
    background: var(--danger-soft);
    box-shadow: 14px 0 0 var(--border-strong), 28px 0 0 var(--border-strong);
    opacity: 0.8;
  }

  .panel-after .transform-panel-head::before {
    background: var(--accent);
    box-shadow: 14px 0 0 var(--border-strong), 28px 0 0 var(--border-strong);
    opacity: 0.8;
  }

  .transform-panel-head span {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text-dim);
  }

  .tag {
    font-family: var(--font-mono);
    font-size: 0.6rem;
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    padding: 0.18rem 0.5rem;
    flex-shrink: 0;
    border: 1px solid var(--ink);
    box-shadow: 2px 2px 0 var(--ink);
  }

  .tag-red {
    background: color-mix(in srgb, var(--danger) 20%, var(--white) 80%);
    color: var(--danger-ink);
  }

  .tag-green {
    background: color-mix(in srgb, var(--accent) 34%, var(--white) 66%);
    color: var(--accent-ink-strong);
  }

  .transform-panel-body {
    padding: 1.25rem;
  }

  .t-row {
    display: flex;
    align-items: baseline;
    gap: 0.75rem;
    padding: 0.5rem 0;
    font-size: 0.84rem;
  }

  .t-row + .t-row {
    border-top: 1px solid var(--border);
  }

  .t-icon {
    width: 18px;
    height: 18px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    font-size: 0.6rem;
    position: relative;
    top: 2px;
    border: 1px solid;
  }

  .t-icon-bad {
    background: color-mix(in srgb, var(--danger) 16%, var(--white) 84%);
    color: var(--danger-ink-strong);
    border-color: color-mix(in srgb, var(--danger) 35%, var(--border) 65%);
  }

  .t-icon-good {
    background: color-mix(in srgb, var(--accent) 22%, var(--white) 78%);
    color: var(--accent-ink-solid);
    border-color: color-mix(in srgb, var(--accent) 50%, var(--border) 50%);
  }

  .t-row .label {
    color: var(--text);
    font-weight: 600;
  }

  .t-row .desc {
    color: var(--text-dim);
  }

  @media (max-width: 900px) {
    .transform-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
