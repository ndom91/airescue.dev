<div class="container">
  <footer>
    <div class="footer-inner">
      <span class="mono">&copy; 2026 AIrescue.dev</span>
      <div class="footer-links">
        <a href="#">Twitter</a>
        <a href="#">GitHub</a>
        <a href="#">Terms</a>
        <a href="#">Privacy</a>
      </div>
    </div>
  </footer>
</div>

<style>
  footer {
    padding: 2.8rem 0;
  }

  .footer-inner {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 0.75rem;
    color: var(--text-dim);
    border-top: 1px solid var(--border);
    padding-top: 1.2rem;
  }

  .footer-links {
    display: flex;
    gap: 1.2rem;
  }

  .footer-links a {
    color: var(--text-dim);
    text-decoration: none;
    font-family: var(--font-mono);
    text-transform: uppercase;
    letter-spacing: 0.03em;
    transition: color 0.2s ease;
  }

  .footer-links a:hover {
    color: var(--text);
  }

  @media (max-width: 720px) {
    .footer-inner {
      flex-direction: column;
      align-items: flex-start;
      gap: 0.8rem;
    }
  }
</style>
