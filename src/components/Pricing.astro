<section class="pricing-section">
  <div class="container" id="pricing">
    <div class="pricing">
      <div class="section-header pricing-header">
        <div class="eyebrow">// pricing</div>
        <h2>Flat rate. No hourly surprises.</h2>
        <p class="pricing-subtitle">Every project is different, but your scope and spend stay explicit.</p>
      </div>
      <div class="pricing-grid">
        <div class="pricing-card">
          <div class="micro-tag">Fast triage</div>
          <div class="tier">Diagnostic</div>
          <div class="price">$499</div>
          <div class="price-sub">One-time audit</div>
          <ul>
            <li>Full codebase review</li>
            <li>Architecture analysis</li>
            <li>Security vulnerability scan</li>
            <li>Prioritized fix plan</li>
            <li>30-min walkthrough call</li>
          </ul>
          <a href="#" class="btn btn-ghost btn-full">Book diagnostic</a>
        </div>
        <div class="pricing-card featured">
          <div class="micro-tag">Best value</div>
          <div class="tier">Full Upgrade</div>
          <div class="price">$2,999</div>
          <div class="price-sub">Complete upgrade, 5&ndash;10 days</div>
          <ul>
            <li>Everything in Diagnostic</li>
            <li>Complete code refactoring</li>
            <li>Test suite + CI/CD setup</li>
            <li>Documentation</li>
            <li>2 weeks post-delivery support</li>
          </ul>
          <a href="#" class="btn btn-accent btn-full">Start upgrade &rarr;</a>
        </div>
        <div class="pricing-card">
          <div class="micro-tag">Long-term cover</div>
          <div class="tier">Retainer</div>
          <div class="price">$999<span class="price-mo">/mo</span></div>
          <div class="price-sub">Ongoing safety net</div>
          <ul>
            <li>Weekly code reviews</li>
            <li>Priority support queue</li>
            <li>Architecture consulting</li>
            <li>AI workflow coaching</li>
            <li>Dedicated Slack channel</li>
          </ul>
          <a href="#" class="btn btn-ghost btn-full">Get retainer</a>
        </div>
      </div>
      <div class="pricing-footer">
        <div class="pricing-footer-inner">
          <p class="disclaimer">Demo site &mdash; prices, testimonials &amp; stats are illustrative</p>
          <div class="footer-divider"></div>
          <p class="inquiry">Seriously interested? <span class="email">info[at]airescue.dev</span></p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .pricing-section {
    background: transparent;
  }

  .pricing {
    padding: 5.5rem 0 4rem;
    animation: fadeSlideIn ease both;
    animation-timeline: view();
    animation-range: entry 0% entry 20%;
  }

  .pricing-header {
    text-align: left;
    margin-bottom: 4rem;
  }

  .pricing-header h2 {
    max-width: 680px;
  }

  .pricing-subtitle {
    font-size: 0.96rem;
    color: var(--text-dim);
    margin-top: 0.75rem;
    line-height: 1.6;
  }

  .pricing-grid {
    display: grid;
    grid-template-columns: 1fr 1.15fr 1fr;
    gap: 1.15rem;
    align-items: start;
  }

  .pricing-card {
    border: 2px solid var(--ink);
    padding: 2rem;
    display: flex;
    flex-direction: column;
    background: var(--surface);
    transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1), box-shadow 0.3s, border-color 0.3s;
    box-shadow: 6px 6px 0 var(--border-strong);
    position: relative;
  }

  .pricing-card:not(.featured):hover {
    transform: translate(-3px, -3px);
    box-shadow: 9px 9px 0 var(--ink);
    border-color: var(--text);
  }

  .pricing-card:nth-child(1) {
    transform: translateY(0.6rem);
  }

  .pricing-card:nth-child(3) {
    transform: translateY(1.2rem);
  }

  .pricing-card:nth-child(1):hover {
    transform: translate(-3px, calc(0.6rem - 3px));
  }

  .pricing-card:nth-child(3):hover {
    transform: translate(-3px, calc(1.2rem - 3px));
  }

  .pricing-card.featured {
    transform: translateY(-0.5rem);
    position: relative;
    z-index: 1;
    box-shadow: 7px 7px 0 var(--ink);
    border-width: 2px;
    background: var(--surface);
    border-color: var(--ink);
  }

  .pricing-card.featured::after {
    content: '';
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0;
    height: 6px;
    background: var(--accent);
  }

  .pricing-card.featured::before {
    content: 'Most selected';
    position: absolute;
    top: -0.85rem;
    left: 1.2rem;
    font-family: var(--font-mono);
    font-size: 0.65rem;
    font-weight: 500;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    color: var(--text);
    background: var(--accent);
    padding: 0.25rem 1rem;
    border: 1px solid var(--ink);
    box-shadow: 2px 2px 0 var(--ink);
  }

  .micro-tag {
    display: inline-block;
    align-self: flex-start;
    margin-bottom: 1rem;
    font-family: var(--font-mono);
    font-size: 0.58rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--text-dim);
    padding: 0.15rem 0.4rem;
    border: 1px solid var(--border-strong);
    background: color-mix(in srgb, var(--surface) 68%, var(--surface-2) 32%);
  }

  .pricing-card.featured .micro-tag {
    color: var(--accent-ink-strong);
    border-color: var(--ink);
    background: var(--accent);
  }

  .pricing-card .tier {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--text-dim);
    letter-spacing: 0.05em;
    text-transform: uppercase;
    margin-bottom: 1.25rem;
  }

  .pricing-card.featured .tier {
    color: var(--accent-ink);
  }

  .pricing-card .price {
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--text);
    letter-spacing: -0.04em;
    line-height: 1;
    margin-bottom: 0.25rem;
    font-variant-numeric: tabular-nums;
  }

  .price-mo {
    font-size: 0.85rem;
    font-weight: 400;
    color: var(--text-dim);
  }

  .pricing-card .price-sub {
    font-size: 0.85rem;
    color: var(--text-dim);
    margin-bottom: 1rem;
  }

  .slot-note {
    margin-bottom: 1.35rem;
    font-size: 0.64rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--text);
    padding: 0.28rem 0.45rem;
    border: 1px solid var(--ink);
    background-image: repeating-linear-gradient(-45deg, var(--stripe-light), var(--stripe-light) 8px, var(--bg) 8px, var(--bg) 16px);
  }

  .pricing-card ul {
    list-style: none;
    flex: 1;
    margin-bottom: 2rem;
  }

  .pricing-card ul li {
    font-size: 0.85rem;
    color: var(--text);
    padding: 0.5rem 0;
    border-bottom: 1px dashed var(--border);
    display: flex;
    align-items: center;
    gap: 0.65rem;
  }

  .pricing-card ul li::before {
    content: '';
    width: 4px;
    height: 4px;
    background: var(--text);
    flex-shrink: 0;
  }

  .pricing-footer {
    margin-top: 3rem;
    border: 1px solid var(--border);
    padding: 1rem 1.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background: color-mix(in srgb, var(--surface) 90%, transparent);
    box-shadow: 5px 5px 0 color-mix(in srgb, var(--border) 50%, transparent);
  }

  .pricing-footer-inner {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    font-family: var(--font-mono);
    font-size: 0.68rem;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .footer-divider {
    width: 1px;
    height: 1rem;
    background: var(--border-strong);
    flex-shrink: 0;
  }

  .disclaimer {
    color: var(--text-dim);
    text-wrap: balance;
  }

  .inquiry {
    color: var(--text-dim);
    text-wrap: balance;
  }

  .inquiry .email {
    color: var(--text);
    border-bottom: 1px solid var(--border-strong);
  }

  @media (max-width: 720px) {
    .pricing {
      padding: 3rem 0 2.5rem;
    }

    .pricing-header {
      margin-bottom: 2.5rem;
    }
  }

  /* 2-column intermediate layout */
  @media (max-width: 1060px) {
    .pricing-grid {
      grid-template-columns: 1fr 1fr;
    }

    .pricing-card:nth-child(1),
    .pricing-card:nth-child(3),
    .pricing-card.featured {
      transform: none;
    }

    /* Reorder: Diagnostic & Retainer on row 1, Featured spans row 2 */
    .pricing-card:nth-child(1) {
      order: 1;
    }

    .pricing-card:nth-child(3) {
      order: 2;
    }

    .pricing-card.featured {
      order: 3;
      grid-column: 1 / -1;
    }
  }

  /* Single-column mobile layout */
  @media (max-width: 600px) {
    .pricing-grid {
      grid-template-columns: 1fr;
    }

    /* Reset order for single column — featured first */
    .pricing-card:nth-child(1) {
      order: 0;
    }

    .pricing-card:nth-child(3) {
      order: 0;
    }

    .pricing-card.featured {
      order: -1;
      grid-column: auto;
    }

    .pricing-footer-inner {
      flex-direction: column;
      gap: 0.5rem;
      text-align: center;
    }

    .footer-divider {
      width: 3rem;
      height: 1px;
    }
  }
</style>
